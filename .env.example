# ========================================
# SECRETS (keep in .env)
# ========================================

PRIVATE_KEY=your-private-key

# Use INFO/DEBUG as per need
SDK_LOG_LEVEL=DEBUG

RPC_URL=
RPC_WS_URL=


# Comment out monadLogs if you want to only get committed state
KURU_RPC_LOGS_SUBSCRIPTION=monadLogs

# Multiplier used with respect to estimated gas for transaction placement
KURU_GAS_BUFFER_MULTIPLIER=1.3

# ========================================
# OPERATIONAL CONFIGURATION
# ========================================
# Operational configuration has moved to bot_config.toml
# See bot_config.example.toml for all strategy parameters
#
# Legacy fallback: If bot_config.toml doesn't exist, the bot will
# read these parameters from .env (hot-reload will be disabled)
#
# Uncomment below for legacy .env mode:
# ORACLE=coinbase
# COINBASE_SYMBOL=MON-USD
# MAX_POSITION=10000
# OVERRIDE_START_POSITION=
# RECONCILE_INTERVAL=300
# PROP_SKEW_ENTRY=0.5
# PROP_SKEW_EXIT=0.5
# PROP_MAINTAIN=0.2
# QUANTITY=400
# QUOTERS_BPS=1,10,15
